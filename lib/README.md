# lib

Managed assemblies the engine needs that are not on NuGet: the CAPE-OPEN interop, and anything
else that ships as a binary. Everything with a NuGet package belongs in `Directory.Packages.props`
instead.
