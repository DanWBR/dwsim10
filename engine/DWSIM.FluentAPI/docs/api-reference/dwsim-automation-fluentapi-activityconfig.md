# ActivityConfig

`DWSIM.Automation.FluentAPI.ActivityConfig`

Shared helper for activity-coefficient model binaries (NRTL / UNIQUAC). Reflects on the inner model's `InteractionParameters` dictionary and writes named scalar fields on the IP record.
