# Builders.Bioprocess.ChromatographyBuilder

`DWSIM.Automation.FluentAPI.Builders.Bioprocess.ChromatographyBuilder`

Fluent builder for the Chromatography unit operation. Call `AddChromatography` to obtain one.

## Methods

### `WithChemistry(DWSIM.UnitOperations.UnitOperations.ChromatographyChemistry)`

Sets `Chemistry` and returns this builder for chaining.

### `WithColumnVolumeLiters(double)`

Sets `Column Volume Liters` and returns this builder for chaining.

### `WithDefaultRecoveryToProduct(double)`

Sets `Default Recovery To Product` and returns this builder for chaining.

### `WithDynamicBindingCapacityGPerL(double)`

Sets `Dynamic Binding Capacity GPer L` and returns this builder for chaining.

### `WithLoadingTime(Quantity)`

Sets `Loading Time` (SI) and returns this builder for chaining.

### `WithMode(DWSIM.UnitOperations.UnitOperations.ChromatographyMode)`

Sets `Mode` and returns this builder for chaining.

### `WithRecoveryToProduct(string, double)`

Sets `Recovery To Product` and returns this builder for chaining.

### `WithResinDensityGPerL(double)`

Sets `Resin Density GPer L` and returns this builder for chaining.

### `WithThomasRateConstantLPerGS(double)`

Sets `Thomas Rate Constant LPer GS` and returns this builder for chaining.
